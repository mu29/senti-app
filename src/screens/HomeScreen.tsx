import React from 'react';
import { StoryList, ReplyModal } from 'components';

const HomeScreen = () => (
  <React.Fragment>
    <StoryList />
    <ReplyModal />
  </React.Fragment>
);

export default HomeScreen;
